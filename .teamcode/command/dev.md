---
description: "Start the development server with live reload"
---

Run the Go application in development mode.

Uses `air` for live reload (if installed), otherwise `go run .`:

```bash
air .
# or
go run .
```
